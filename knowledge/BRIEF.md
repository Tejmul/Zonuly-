# ZoNuLy — context brief

> Generated 2026-08-31T00:12 from the knowledge graph (1728 nodes, 3331 edges). Do not edit — run `python scripts/run.py kg brief`.
> To query: `kg search <text>` · `kg show <id>` · `kg path <a> <b>` · `kg compose "<problem>"` · to remember: `kg note "<what changed>" --about <id>`.

## The problem

An engineer on site in the US/UK/Germany costs ₹1.5–2 crore a year; the same company can hire us remotely from India, or on a visa, for ₹20–50 lakh and not notice. Recently funded startups exploit that gap; the hyped names do not. We are two final-year students with real shipped AI/infra work and normal (not Codeforces) DSA. ZoNuLy finds those companies and roles, grades each against our resume, finds 10–30 real people per company and labels who can refer, writes one honest email each, stops for our approval, sends 25 a day, tracks replies, follows up once, and puts every "yes" on our calendar. Judgement only matters at "yes, send that to that person". Full statement: knowledge/MOTIV.md.


- **You never see most of the good jobs** — Funded startups post to their own careers page (an ATS) and wait; aggregators see the role after hundreds have applied.
- **You can't tell which ones are worth your time** — "AI Engineer" spans eight-years-of-ML to founding-engineer-for-a-strong-student; only reading the JD against the resume tells you which. Grade decides order and effort, never whether we try.
- **Cold applications go nowhere** — A portal submission is one of 800; a weak referral from an engineer moves you to a stack of ten.
- **Referrals don't scale by hand** — Find a person, their role, their email, their work; write something non-templated; send; follow up; schedule — twenty minutes each, and we have coursework.
- **The pay gap is geographic and the target is funded startups abroad** — US/UK/DE remote-from-India or on-visa at any well-paid local rate; India on-site only at ₹30 LPA+. Not Google/Microsoft/Amazon. Visa feasibility (DE > UK > US) must be scored, not assumed.
- **Who can actually refer** — 10–30 people per company, each classified SDE1/2/3, staff, EM, DevOps, founder/CTO, recruiter/HR. Engineers and EMs first, founders next, recruiters last.
- **A "yes" must not get lost** — Replies that send a Meet link, an assessment or an interview slot go on our calendar with a notification; two companies on one slot get a reschedule draft for approval.
- **Two of us, one pipeline** — Per-candidate resumes, scores, drafts, Gmail accounts and send budgets; shared companies, jobs and contacts; never both of us to the same person in the same month.

## Where the pipeline is right now (live)

- **1100** jobs from 7 sources (ashby 517, greenhouse 348, hn 132, wwr 79, yc 11, lever 10, remoteok 3)
- **102** scored · status: high_match 26, new 998, scored 76
- **263** companies · **25** contacts (verified 25)
- emails: draft 1 · replies: none
- top matches: [75] Software Engineer, Early Career (AI) @ Notion (job:693); [75] Software Engineer - Product @ Luma AI (job:793); [75] Applied AI Engineer @ WorkOS (job:865); [75] LLM Engineer Freelancer @ Monterail (job:1035); [75] AI Automation Engineer, In-House MarTech Video SaaS @ Vidalytics (job:1047); [72] Software Engineer, Forward Deployed AI @ Ramp (job:658); [72] Software Engineer, AI Workflows @ Notion (job:689); [72] Product Engineer - Dedicated Inference  @ Baseten (job:755)

## Constraints and guarantees (never cut)

- `constraint:send-cap-25` **25 emails a day, each irreversible** — The real ceiling. A personal Gmail sending 200 lookalike messages gets flagged, and the flagged account is the one interviews arrive at. Every upstream decision is about spending 25 well.
- `constraint:human-review` **Nothing sends without a human reading it** — The review queue is the only path to send. Chosen mode, not a default.
- `constraint:local-only` **(retired) Model runs on the laptop; no LLM API key** — Retired 2026-08-31 by D18. Replaced by — no secret leaves the laptop, every outbound model call is logged, priced and capped. The resume and drafts do go to OpenRouter.
- `constraint:no-local-model` **No Ollama, no local model, ever** — Every attempt to run Ollama locally broke the system. All model calls go through OpenRouter behind a ledger and caps; a CI grep fails on any ollama import.
- `constraint:8gb-ram` **The machine has 8 GB of RAM and no model runtime** — Ollama wrecked the machine every time. With no local model the pipeline is I/O-bound and 8 GB is plenty; the constraint now means no daemons and no local inference of any kind.
- `constraint:free-tier` **Free tiers only for data APIs** — No paid Apollo/SerpAPI/Findymail/MillionVerifier. Revisit only if verified-contact rate drops below ~20%.
- `constraint:no-linkedin-automation` **No logged-in LinkedIn automation, ever** — A banned LinkedIn destroys the exact asset being built — the ability to be referred. LinkedIn is in no scraper module.
- `constraint:single-user-laptop` **One user, one machine, one SQLite file** — No Postgres, no daemons, no cloud scheduler. Everything that exists because of multi-tenancy is out.
- `constraint:ten-minute-review` **Daily review must take ~10 minutes** — If review takes forty minutes the system has failed at its only human step. Unmeasured so far.
- `constraint:context-continuity` **A new session must not start from scratch** — Decisions, gaps and what-changed-when must be queryable, not re-derived from six documents every time. This is what the knowledge graph is for.
- `constraint:student-budget` **₹0 recurring; at most ₹1,000–2,500/month if paid data raises who we reach** — We are students. Every paid call sits behind a daily budget gate and a cache; the baseline must work at zero.
- `constraint:visa-reality` **Visa feasibility is scored, never assumed** — US on-site for a new grad is an H-1B lottery; UK needs a sponsor licence; Germany's Blue Card is feasible with a degree and a €48k+ offer. Remote-from-India is the realistic first path abroad.
- `constraint:one-of-us-per-contact` **Never both of us to the same person in the same month** — Two near-identical emails from two students at one college a week apart reads as spam and burns the company for both.
- `constraint:approval-first` **Nothing happens without permission — in the app and in building it** — Every action has a tier (local read · free external read · spend · write to the world · forbidden). Spend and world-writes need a grant; anything without one creates a request and waits. Building follows the same rule — one proposed, approved step at a time (FINAL-PLAN-V2 §13–14).
- `guarantee:human-gate` **Nothing sends without a human** — sender.send_email() refuses any email whose status isn't approved; approval is only settable from the approve route; no scheduled job calls send. _(enforced by: outreach/, Review queue — edit · approve · reject; the only path to send)_
- `guarantee:no-fabrication` **Nothing is invented** — Never a project, number or shared connection that isn't in the source material. Currently prompt-level only — the mechanical check is GAP-2. _(enforced by: Researcher reads their repos + bio → one honest hook, or null)_
- `guarantee:budget-spent-well` **25/day, irreversibly spent, visible everywhere** — sent_today() counts sent rows since local midnight and gates every send. A count, not yet a ledger — GAP-1. _(enforced by: Daily cap as a count — sent rows since local midnight)_
- `guarantee:confidence-labels` **Every contact carries a confidence tier, never collapsed** — verified | pattern-guessed | scraped, stated on every insert, shown as a badge everywhere, used to order sends. _(enforced by: verified | pattern-guessed | scraped — never defaulted, never collapsed, db.py)_
- `guarantee:one-followup` **At most one follow-up per thread** — Email.followup_sent is set when the follow-up is queued, not sent, so a second can't be created even if the first is never approved. _(enforced by: Exactly one follow-up after 5 silent days, back through the review queue)_
- `guarantee:data-stays-local` **No secret leaves the laptop; every outbound call is logged, priced and capped** — Revised 2026-08-31 (D18). The resume and drafts do go to OpenRouter; keys stay in the environment; only the four network adapters may call out; every paid call is a ledger row under a cap you set. _(enforced by: OpenRouter-only model layer — aliases cheap / judge / writer, num_retries 0, think-stripping, a cost row per call, budget gate, no key → stage does not run, agent_run + agent_run_cost + apify_run_log — every invocation a row with phases, status, tokens, INR, error, parent_run_id, CI greps and tests — no ollama import, no network outside the four adapters, no untimed wait, Apify Bearer only, no AUTO_REJECT, EXECUTE_OUTREACH unheld, every tool has a capability, every constant pinned)_
- `guarantee:no-auto-apply` **No auto-apply, no contact-list building** — There is no application-submission code path and no export path. One person, one role, one ask.

## Settled decisions

- **D1 — Python, not TypeScript** (`decision:d1-python`) — The value of the system sits in the scraping, PDF and email ecosystem, which is Python's. The frontend stays Next.js over REST. Consequence — the portal's layering lint doesn't port; the rule holds by convention.
  - adopts: Python pipeline + Next.js UI over REST
  - rejects: Next.js + Drizzle + Node worker for everything
- **D2 — (reversed by D18) qwen3:4b, the size the machine can actually hold** (`decision:d2-model-size`) — REVERSED 2026-08-31 — Ollama wrecks the machine; see D18. Every draft assumed 14B and 16 GB; the machine has 8. 4B ranks correctly on real postings (verified by hand); calibration against real screens is still unmeasured. Fixed num_ctx + keep_alive 30m are load-bearing.
  - adopts: qwen3:4b + nomic-embed-text, resident together
  - rejects: qwen2.5:14b scoring + small model tiering
- **D3 — (reversed by D18) No hosted model; local wins** (`decision:d3-no-hosted-model`) — REVERSED 2026-08-31 — OpenRouter is the only provider; see D18. TOOLS budgeted Claude for drafts; ORCHESTRATION said "there is no LLM API key, and that is the point." Local wins — it was the original constraint, the review queue catches weak prose, and llm.py is a one-line seam. Pull that seam first if reply rates are bad.
  - adopts: Drafts written by the local model — 90–130 words, plain text; One thin llm.py — the only place a model is called
  - rejects: Claude Sonnet for drafts (local for everything else)
- **D4 — The orchestrator is code; there are no supervisor agents** (`decision:d4-deterministic-orchestrator`) — Every supervisor decision is a ten-line policy with a config value. What survived — the state machine, narrow workers, typed contracts, depth cap, fault isolation. What was dropped — envelopes, capability narrowing, the run tree.
  - adopts: Deterministic orchestrator — APScheduler + per-item commits, no LLM supervisors; Typed contracts between stages (RawJob, Salary, Score); One dead source never kills a run
  - rejects: 5 LLM supervisors over 18 workers with typed envelopes; Per-source circuit breaker with a source_health table; Append-only artifacts with content_hash; everything else re-derivable
- **D5 — The GitHub email spike came back good, bimodally** (`decision:d5-github-spike`) — Supabase 12/12, Affirm 20/20, GitLab 5/5, Observe.AI 0 (no org). Person-event mining yields nothing (GitHub removed commit detail from PushEvent); the repo commits endpoint still carries author emails. Infra/devtools companies are cheap to contact; companies without public code should be skipped, not guessed at.
  - adopts: GitHub repo-commits endpoint → commit.author.email; No public code and no team page → skip the company, don't guess
- **D6 — Free tier only; no paid finders or verification credits** (`decision:d6-free-tier`) — The waterfall can take paid tiers but free tiers are doing the work. Hunter's 25/mo is reserved for learning a company's pattern. Revisit if verified rate drops below ~20%.
  - adopts: Hunter.io free tier (25/mo) spent on patterns, not people; Learn the company's email pattern, apply it to every other name, MX-check
  - rejects: Findymail / Anymailfinder + MillionVerifier credits
- **D7 — SMTP RCPT probing is off; TOOLS.md was right** (`decision:d7-smtp-off`) — Built on by default, which was wrong. Port 25 is blocked residentially, big providers accept every recipient at the edge, and probing risks an IP listing. MX lookup stays.
  - adopts: MX lookup always; SMTP RCPT probe off
  - rejects: SMTP RCPT verification probe
- **D8 — Three confidence tiers, never collapsed** (`decision:d8-three-tiers`) — verified / pattern-guessed / scraped, named for what the user needs to distinguish. Shown everywhere, never defaulted.
  - adopts: verified | pattern-guessed | scraped — never defaulted, never collapsed
- **D9 — SQLite, one file, no vector extension** (`decision:d9-no-sqlite-vec`) — Plain-Python cosine over cached vectors is sub-second at 1,100 postings. Revisit past ~50k.
  - adopts: SQLite, one file, no daemon; Resume vector cached in a Setting row, invalidated by content hash
  - rejects: sqlite-vec extension for embeddings
- **D10 — The knowledge graph lives in SQLite with NetworkX on top, not in a graph database** (`decision:d10-kg-in-sqlite`) — Same logic as D9 — Neo4j needs a daemon, Kùzu was archived in late 2025, LLM-memory frameworks want a server-class backend. Two tables and an FTS5 index in jobhunter.db keep it in the one file that's backed up; NetworkX gives centrality, paths and GraphML export in memory.
  - adopts: Knowledge graph — data + context in one queryable store
- **D11 — SQLite stays; CSV is an interface, not the store** (`decision:d11-sqlite-not-csv`) — The restated problem asked for CSV and no database. A CSV cannot hold dedup keys, scores, contacted-when, budgets or reply state. Export and import CSVs exist for the two of us; the state stays in jobhunter.db.
  - adopts: CSV export of companies / contacts / sends / events, CSV import of companies through the audit gate
  - rejects: Store companies in a CSV instead of a database
- **D12 — LinkedIn data comes from public no-cookies channels or not at all** (`decision:d12-linkedin-no-cookies-only`) — Free path first (GitHub commits, site, pattern). Where that yields nothing at a graded company, an Apify no-cookies employee harvest at ~$0.15/company behind a $0.50/day gate. Our own LinkedIn is used by hand for the alumni filter and never automated.
  - adopts: Apify no-cookies LinkedIn employee harvest, budget-gated at $0.50/day, cached 24h; Apify response cache keyed on (actor, version, input hash) with TTL, plus a daily cost ledger
- **D13 — "Multi-agent" means narrow LLM workers under a deterministic conductor** (`decision:d13-agents-are-workers`) — Salary extractor, fit judge, role classifier, researcher, drafter, reply classifier, schedule extractor, follow-up drafter. Each has a typed input and output and a budget; none calls another; none can send; deterministic code decides what runs. D4 restated with a roster.
  - adopts: Role classifier — engineer | senior_engineer | manager | founder | recruiter | other, plus seniority; Schedule extractor — a positive reply becomes {intent, proposed_times, link, deadline, needs_action}; Deterministic orchestrator — APScheduler + per-item commits, no LLM supervisors
- **D14 — Target regions and pay are config; the scorer knows about visa feasibility** (`decision:d14-regions-and-feasibility`) — search.regions with per-region floors; a fifth rubric dimension for remote-from-India / sponsorship / new-grad visa realism; a hard cap on US on-site with no sponsorship statement.
  - adopts: Target regions with per-region pay floors — US/UK/DE any well-paid, India ≥ ₹30 LPA on-site; Fifth rubric dimension — does this posting allow remote-from-India, sponsor, or sit where a new-grad visa is realistic
- **D15 — Two candidates, one pipeline, one of us per contact per month** (`decision:d15-two-candidates`) — Per-candidate scores, drafts, sends, budgets and Gmail; shared companies, jobs and contacts; the cooldown is enforced in the drafter.
  - adopts: Two candidates, one pipeline — candidate table, per-candidate scores, drafts, Gmail and budgets; A contact is written to by at most one of us per 30 days; one of us per company per role
- **D16 — OpenRouter for the writing tasks, local for everything else, both behind the permission gate** (`decision:d16-openrouter-writing-tier`) — Revises D3. Drafts, follow-ups and the approval assistant may run on a cheap hosted model via one OpenRouter key (≤ 80 calls/day, ≤ ₹400/month); scoring, embeddings, research and reply classification stay local; any failure or a missing key falls back to local. Disabled until enabled in permissions.yaml.
  - adopts: Per-task routing in llm.py — draft / followup / review to OpenRouter, score / reply / research local, local fallback always; Approval assistant — a review worker that flags each draft for fabrication, tone, length and the one-ask rule; never approves
  - rejects: Claude Sonnet for drafts (local for everything else)
- **D17 — Nothing runs or gets built without permission** (`decision:d17-approval-first`) — The permission gate is build step 0 and every later step is gated by it; the build itself proceeds one approved step at a time with a note after each.
  - adopts: Permission gate — tiers 0–4, permissions.yaml grants, approval_request + action_log, pending/grant/deny CLI and dashboard tab; action_log — every tier 1–3 call with cost, candidate and outcome; Build protocol — every step proposed with scope, files, tools, cost, verification and rollback; approved by number; one at a time; kg note after each
- **D18 — No Ollama, no local model; OpenRouter is the only model provider** (`decision:d18-no-local-model`) — Reverses D2 and D3. Ollama has wrecked the machine every time. Aliases cheap / judge / writer over one key, num_retries 0, a cost row per call, daily and monthly caps in permissions.yaml, the lexical fit gate at the wide end of the funnel so the model only reads plausible jobs.
  - adopts: OpenRouter-only model layer — aliases cheap / judge / writer, num_retries 0, think-stripping, a cost row per call, budget gate, no key → stage does not run; Model-free lexical fit — rarity-weighted vocabulary overlap between resume and JD, stemmed, stop-listed; Triage agent on the cheap alias — worth scoring? region? remote? sponsorship stated?
  - rejects: qwen3:4b + nomic-embed-text, resident together; Drafts written by the local model — 90–130 words, plain text; Embedding prefilter by percentile, not threshold; Resume vector cached in a Setting row, invalidated by content hash
- **D19 — Agents are pydantic-ai workers that can only propose; capabilities and budgets are enforced by the runtime, not the prompt** (`decision:d19-agents-propose-runtime-enforces`) — gtm-arch's agent runtime and proposal path, MULTI-AGENT's depth cap and envelopes, without Temporal. Every write is a fact_proposal or draft_proposal; every run is a ledger row; EXECUTE_OUTREACH is held by nobody.
  - adopts: pydantic-ai agent runtime — typed I/O, narrow tool surface, enforce_capability first line of every tool, budget triple, EXECUTE_OUTREACH held by nobody; fact_proposal + draft_proposal — every agent write is a proposal; facts promote at ≥ 0.85, review 0.70–0.85, review-with-reason below; drafts always HITL; never auto-reject; agent_run + agent_run_cost + apify_run_log — every invocation a row with phases, status, tokens, INR, error, parent_run_id; budgets.py — every timeout, threshold and cap in one file, pinned by a test; no AUTO_REJECT symbol; artifact table — every fetched document append-only with content_hash; evidence spans point here; Faithfulness gate (span exists · span supports) + publish gate (every proper noun and number matches a span or profile field) — strip, never rewrite
  - rejects: 5 LLM supervisors over 18 workers with typed envelopes
- **D20 — Three scraping channels behind one politeness core, one staging area, one audit gate, driven by a launchd tick** (`decision:d20-three-channel-scraping`) — CHOKEPOINTS' engine shape — keyless channel at the documented rate 24/7, Apify no-cookies channel behind a budget, Brave research channel returning only cited facts — replaces per-scraper clients and APScheduler.
  - adopts: fetch.py — the only place the network is touched for free sources; robots when real, per-host rate, disk cache by URL hash, real UA, 20 s / 2 MB bounds; CI grep for any other httpx call; Every channel writes staging; promote previews by default, --apply refuses on a failing audit and restores the backup; tick.py under launchd — hourly, fcntl-locked, spends this hour's rate budget and today's money budget, most-stale work first, source_health with backoff, exits in minutes; Brave Search + politeness-core fetch + cheap model → observations and facts that each carry an artifact span; uncited items dropped; source quality classified separately; apify.py — PINNED_ACTORS in ~ form with version, output_model and estimated cost; run-sync endpoint; 30-day cache keyed on (actor, version, input hash); budget pre-flight; ?maxCharge; shape mismatch non-retryable; Bearer only; Daily report — sources at 0, scores, people by channel, drafts owed, sends left, replies by class, events today, money spent by vendor and alias against caps; Every multi-item stage appends and fsyncs per item and skips ids already present on restart — a relaunch never re-spends a paid call
  - rejects: Per-source circuit breaker with a source_health table
- **D21 — Telegram is the second review surface, with the atomic-claim pattern** (`decision:d21-telegram-review`) — zon-arch's Slack cards for two people on a free bot. Decisions are recorded by Telegram and acted on only by the API's approval handler.
  - adopts: Telegram bot — proposal cards with Approve / Edit / Reject / Skip, atomic pending→processing claim, Edit becomes edited_body, budget alerts at 80%, morning report

## What exists, by stage

### Platform (cross-cutting) — Storage, model client, orchestration, API, tests — what every stage stands on.
- **[DROPPED]** 5 LLM supervisors over 18 workers with typed envelopes — Capability narrowing, run tree, budget triples. Solves untrusted delegation, which a pipeline of plain function calls doesn't have. Dropped in D4; the narrow-worker and typed-contract ideas survived. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** API never runs long work inline — task_id + polling — Scrape, score, discovery and send dispatch to a thread; the dashboard polls /api/tasks/{id}. Copied wholesale from the portal; why the UI never blocks on a 20-second model call. _(in jobhunter.api)_ _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, FINAL-PLAN.md — the reconciliation)_
- **[GAP]** Apify response cache keyed on (actor, version, input hash) with TTL, plus a daily cost ledger — Re-running a night never re-spends; the $0.50/day gate reads the ledger before every call. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, zon-arch.md — the MVRX portal)_
- **[DROPPED]** Append-only artifacts with content_hash; everything else re-derivable — gtm-os's episode layer. Dropped — the job table refreshes volatile fields only and never clobbers scoring work, which covers the real need. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** Build protocol — every step proposed with scope, files, tools, cost, verification and rollback; approved by number; one at a time; kg note after each — No new tool, key, dependency or spend without a line in the inventory and a yes. Anything touching sending, budgets or the permission file is shown as a diff before it runs. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[GAP]** CI greps and tests — no ollama import, no network outside the four adapters, no untimed wait, Apify Bearer only, no AUTO_REJECT, EXECUTE_OUTREACH unheld, every tool has a capability, every constant pinned — gtm-arch §16 custom guardrails, adapted. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model)_
- **[GAP]** CSV export of companies / contacts / sends / events, CSV import of companies through the audit gate — The CSV the problem statement asked for, as an interface for the two of us and for spreadsheets — not as the store (D11). _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[GAP]** Daily report — sources at 0, scores, people by channel, drafts owed, sends left, replies by class, events today, money spent by vendor and alias against caps — A silent partial run is worse than a loud one. _(from: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[PARTIAL]** Deterministic orchestrator — APScheduler + per-item commits, no LLM supervisors — Every "supervisor decision" is a ten-line policy with a config value. A crash loses one item, not the run. Daily 08:00 cycle, hourly reply poll, sequential on 8 GB. _(in jobhunter.scheduler)_ _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal)_
- **[GAP]** Every multi-item stage appends and fsyncs per item and skips ids already present on restart — a relaunch never re-spends a paid call — CHOKEPOINTS §13.1. _(from: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit)_
- **[GAP]** Fixture-driven tests — salary table, recorded scraper JSON, hand-labelled scores — "Non-negotiable from commit one" in the drafts; there are none. Salary parsing already caught two bugs by hand and is the cheapest insurance. _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing)_
- **[OPEN]** Hard-coded egress allowlist in the fetch wrapper — "Your data doesn't leave the laptop" as a code path. Today it holds by module convention — each scraper only knows its own host. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[GAP]** Hourly bounded tick under launchd, fcntl-locked — the pipeline runs while the API is down and fires on wake — APScheduler inside the API misses 08:00 whenever the laptop is asleep at 08:00. One slice per hour — scrape one source, or score N, or find contacts for one company, whichever is most stale. _(from: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit)_
- **[BUILT]** Knowledge graph — data + context in one queryable store — SQLite nodes/edges/FTS in jobhunter.db, NetworkX for analysis; mirrors the tables, seeds the context from knowledge/context.yaml, renders knowledge/BRIEF.md, composes architectures per problem statement. _(in jobhunter.kg)_
- **[GAP]** Layering rule enforced by a lint script — db/llm/normalize ← scrapers/contacts/outreach ← pipeline/matcher ← api ← dashboard. Holds by convention today; the portal's lint-architecture.sh doesn't port directly. _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing)_
- **[DROPPED]** Next.js + Drizzle + Node worker for everything — One language, knowledge transfers from the MVRX portal. Lost to D1 — the value sits in Python's scraping and email libraries. _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing)_
- **[PARTIAL]** One thin llm.py — the only place a model is called — Chat, JSON mode with parse-retry, embeddings. Swapping to a bigger local model or a hosted one is a config line. Pull this seam first if reply rates are bad. _(in jobhunter.llm)_ _(from: PLAN.md — the original build plan, FINAL-PLAN.md — the reconciliation)_
- **[GAP]** OpenRouter-only model layer — aliases cheap / judge / writer, num_retries 0, think-stripping, a cost row per call, budget gate, no key → stage does not run — Replaces Ollama entirely. Model ids live only in config; call sites use aliases; the conductor owns retry; today's date injected into every prompt. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model)_
- **[GAP]** Permission gate — tiers 0–4, permissions.yaml grants, approval_request + action_log, pending/grant/deny CLI and dashboard tab — Every action is checked; spend needs an enabled provider under cap; world-writes are per-item; forbidden has no code path. Anything without a grant creates a request, notifies, and waits. The scheduler never blocks on a request. Build step 0. _(from: FINAL-PLAN-V2.md — the plan against the restated problem, gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet)_
- **[BUILT]** Python pipeline + Next.js UI over REST — The scraping/PDF/email ecosystem is in Python; the dashboard stays Next.js but owns no data layer. _(in jobhunter.api, dashboard)_ _(from: PLAN.md — the original build plan, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** SQLite, one file, no daemon — Full relational DB for a single user; Postgres would eat RAM Ollama needs. SQLModel makes a later Postgres move a connection-string change. _(in jobhunter.db)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, FINAL-PLAN.md — the reconciliation)_
- **[DROPPED]** Store companies in a CSV instead of a database — Cannot hold dedup keys, scores, who-was-contacted-when, budgets or reply state. CHOKEPOINTS moved its harvest state to SQLite for exactly this reason. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[BUILT]** Structured-output validation with bounded retries — chat_json parses, nudges the model on failure, gives up after 2 retries with a default. GEMINI's tool-loop limit is moot — no agent here loops over tools. _(in jobhunter.llm)_ _(from: GEMINI.md — sequential agents, guardrails, provenance, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal)_
- **[GAP]** Today's date injected into every prompt — Models otherwise reason from their training cutoff. Load-bearing for the schedule extractor — "Thursday 4pm" needs a reference date. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet)_
- **[GAP]** Two candidates, one pipeline — candidate table, per-candidate scores, drafts, Gmail and budgets — job_score(candidate_id, job_id) replaces the score columns on job; email.candidate_id; sent_today() per candidate; companies, jobs and contacts stay shared; a candidate switcher in the dashboard. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[BUILT]** Typed contracts between stages (RawJob, Salary, Score) — Each stage hands the next a small typed payload; no stage sees the whole day. _(in jobhunter.scrapers, jobhunter.normalize, jobhunter.matcher)_ _(from: GEMINI.md — sequential agents, guardrails, provenance, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[GAP]** action_log — every tier 1–3 call with cost, candidate and outcome — "What did the system do last night and what did it cost" is one query. _(from: FINAL-PLAN-V2.md — the plan against the restated problem, CHOKEPOINTS.md — a keyless scraper run at the source's rate limit)_
- **[GAP]** agent_run + agent_run_cost + apify_run_log — every invocation a row with phases, status, tokens, INR, error, parent_run_id — Cost is a column, not a log line. Resumable; the conductor parses output rows, never transcripts. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, zon-arch.md — the MVRX portal, CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[DROPPED]** api_costs table — cost as a column, not a log line — The portal's missing column. Dropped here because the local model costs ₹0; wall-clock is the real budget. _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing)_
- **[GAP]** artifact table — every fetched document append-only with content_hash; evidence spans point here — Revives the dropped idea because the faithfulness gate needs something to point at. Kinds — posting, team_page, repo, profile, search_result, reply. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model)_
- **[GAP]** budgets.py — every timeout, threshold and cap in one file, pinned by a test; no AUTO_REJECT symbol — A silent retune is impossible; a pytest gate asserts the auto-reject constant does not exist. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet)_
- **[GAP]** pydantic-ai agent runtime — typed I/O, narrow tool surface, enforce_capability first line of every tool, budget triple, EXECUTE_OUTREACH held by nobody — gtm-arch §10 in ~300 lines. Depth ≤ 2, no agent calls an agent, a tripped budget returns partial, unknown tool is a hard error. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model)_
- **[DROPPED]** qwen2.5:14b scoring + small model tiering — Every early draft assumed 14B and ~16 GB. Does not fit this machine. _(from: JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[DROPPED]** qwen3:4b + nomic-embed-text, resident together — The best open-weight model that fits 8 GB; ~18 s per rubric score, both models at ~4.2 GB. Fixed num_ctx everywhere, keep_alive 30m, thinking off. _(in jobhunter.llm)_ _(from: PLAN.md — the original build plan, FINAL-PLAN.md — the reconciliation)_
- **[GAP]** tick.py under launchd — hourly, fcntl-locked, spends this hour's rate budget and today's money budget, most-stale work first, source_health with backoff, exits in minutes — The conductor is a bounded slice, not a long-running process. APScheduler removed. A source at 0 twice is a failure in the morning report. _(from: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_

### 1 · Scrape — 11,282 postings from 7 sources, free, parallel, each fault-isolated.
- **[GAP]** A source returning 0 twice running is reported as a failure — The most likely way the system dies without anyone noticing — a board changes shape, the scraper returns 0, nothing errors. per_source is recorded; the alarm isn't. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, FINAL-PLAN.md — the reconciliation)_
- **[BUILT]** ATS public JSON APIs — Greenhouse, Lever, Ashby — Startups embed an ATS; its board endpoint is public JSON. The whole game. 94 boards live-verified in companies.yaml. _(in jobhunter.scrapers)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, GEMINI.md — sequential agents, guardrails, provenance)_
- **[OPEN]** Browse India boards by hand, paste company names into the registry — Five minutes a week, no ToS exposure, and the ATS scrapers pick the companies up. Cheaper and safer than Playwright. _(from: JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, FINAL-PLAN.md — the reconciliation)_
- **[GAP]** Every channel writes staging; promote previews by default, --apply refuses on a failing audit and restores the backup — No scraper writes company / job / contact. Dedup on normalised name and domain root; review the drop list. _(from: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit)_
- **[GAP]** Funding-round RSS → company names → ATS probe → staged registry entries — TechCrunch funding, EU-Startups, Sifted, YourStory are free. A cheap extractor pulls names; discover probes Greenhouse/Lever/Ashby; hits are staged and promoted through an audit. Grows the registry toward recently funded companies, which is the thesis. _(from: FINAL-PLAN-V2.md — the plan against the restated problem, CHOKEPOINTS.md — a keyless scraper run at the source's rate limit)_
- **[BUILT]** HN Who-is-Hiring, RemoteOK, WeWorkRemotely, YC directory — The places a funded 40-person company posts before anyone aggregates it. HN is freeform and needs the model; the rest are feeds. _(in jobhunter.scrapers)_ _(from: PLAN.md — the original build plan, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[GAP]** No scraper writes companies.yaml — discovery stages, promote audits and merges — Dead website, duplicate domain, missing ATS are caught before the registry that compounds is touched. Default is a preview; --apply refuses on a failing audit. _(from: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit)_
- **[BUILT]** One dead source never kills a run — Every scraper is individually try/except'd; ScrapeStats.errors records which failed and per_source counts are kept every run. _(in jobhunter.pipeline)_ _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal)_
- **[DROPPED]** Per-source circuit breaker with a source_health table — Three consecutive failures disable a source for the cycle. Replaced by simple fault isolation — a run is one shot, not a long workflow. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[OPEN]** Playwright scrapers for Wellfound, Cutshort, Instahyre — They carry the Indian salary data the JSON boards don't. Registered and toggled off — slowest, most fragile, ToS exposure. _(in jobhunter.scrapers)_ _(from: PLAN.md — the original build plan)_
- **[BUILT]** companies.yaml — curated, live-probed ATS registry that compounds — ATS APIs are keyed by slug so you need a company list first. `discover` probes YC companies and appends hits. The registry is the real asset. _(in jobhunter.pipeline, jobhunter.scrapers)_ _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, PLAN.md — the original build plan)_
- **[GAP]** fetch.py — the only place the network is touched for free sources; robots when real, per-host rate, disk cache by URL hash, real UA, 20 s / 2 MB bounds; CI grep for any other httpx call — CHOKEPOINTS §9.1.1 verbatim. Read the documented rate and honour it deliberately. _(from: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet)_

### 2 · Filter + dedup — Title/location rules then company+title fingerprint across sources; 1,651 relevant → 1,100 stored.
- **[OPEN]** Canonical company resolution — domain match first, model for the residue — "Acme", "Acme Inc.", "acme.com" should be one company. Today it's name-keyed with a website upsert; never merge on name alone. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** Repair text encoding before anything reads it — Discovered during the build — boards ship mis-decoded UTF-8 that breaks title matching. _(in jobhunter.normalize)_
- **[GAP]** Target regions with per-region pay floors — US/UK/DE any well-paid, India ≥ ₹30 LPA on-site — search.regions replaces the flat location allowlist; gbp_to_inr joins usd/eur; location_ok() classifies country and remote-eligibility instead of matching strings. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[BUILT]** Title + location relevance rules before the database — Cheap rules throw out wrong discipline, seniority and continent before anything expensive touches them. Deliberately generous — a false negative is invisible forever. _(in jobhunter.normalize, jobhunter.pipeline)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** url unique + company+title fingerprint across sources, keep the richest record — The same role on its ATS and an aggregator collapses to one row; 551 duplicates collapsed on the first full run. _(in jobhunter.normalize, jobhunter.pipeline)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_

### 3 · Salary — Regex first, LLM only on the residue; everything normalised to INR LPA.
- **[BUILT]** Salary → INR LPA — regex first, LLM only on the residue — Handles $185,000-$260,000 · ₹25-40 LPA · 30,00,000 INR · £90k · 1.2 Cr · monthly stipends. _(in jobhunter.normalize, jobhunter.pipeline)_ _(from: PLAN.md — the original build plan)_

### 4 · Embed + prefilter — nomic-embed-text cosine vs the resume; keep the top 30% by percentile, ~330 survive.
- **[DROPPED]** Embedding prefilter by percentile, not threshold — nomic-embed-text puts every resume↔JD pair in a 0.55–0.84 band, so an absolute cutoff is meaningless; keep the top 30%. Thresholds drift, percentiles don't. _(in jobhunter.matcher)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[GAP]** Model-free lexical fit — rarity-weighted vocabulary overlap between resume and JD, stemmed, stop-listed — CHOKEPOINTS fit.py applied to jobs. Costs nothing, runs over every posting, replaces the embedding prefilter that needed a local embedder. _(from: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model)_
- **[DROPPED]** Resume vector cached in a Setting row, invalidated by content hash — Plain-Python cosine over cached vectors is sub-second at 1,100 postings. _(in jobhunter.matcher, jobhunter.db)_ _(from: FINAL-PLAN.md — the reconciliation)_
- **[DROPPED]** sqlite-vec extension for embeddings — An extension to install, load and keep compatible for no measurable gain at this scale. Revisit past ~50k postings. _(from: JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_

### 5 · Rubric score — qwen3:4b rubric, ~18 s each → screen odds 0–100 + reasons + gaps; hard rules cap the model; ≥65 is a high match.
- **[BUILT]** A low score ranks a role last; it never deletes it — gtm-os has a pytest gate against any auto-reject constant. The user may know something the rubric doesn't. _(in jobhunter.matcher, jobhunter.api)_ _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** Deterministic caps override the model — senior title 35, below salary floor 40, thin description 60 — The model is trained toward encouragement; the caps aren't. _(in jobhunter.matcher)_ _(from: FINAL-PLAN.md — the reconciliation)_
- **[GAP]** Fifth rubric dimension — does this posting allow remote-from-India, sponsor, or sit where a new-grad visa is realistic — DE > UK > US for on-site; remote contractor is the realistic path for US/UK startups. Hard rule — US on-site with no sponsorship statement caps at 45. Must cite the JD sentence it relied on. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[BUILT]** Rubric scoring — calibrated-pessimistic screen odds + reasons + gaps — Stack overlap, YOE barrier, early-career signal, domain proximity, company stage. A Staff role scores low on perfect overlap; a Series-A founding role scores high on imperfect overlap. Hand-verified on Notion 75 / Deepgram 65 / Observe.AI 35 / Databricks 15. _(in jobhunter.matcher)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, GEMINI.md — sequential agents, guardrails, provenance, IDEA.md — the problem, stated plainly)_
- **[GAP]** Triage agent on the cheap alias — worth scoring? region? remote? sponsorship stated? — The middle of the cost gradient — ₹0.05 per JD — so the judge only reads plausible roles. _(from: FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[GAP]** outcomes table + calibration against real screens — Whether "75" means 75% is unknown until real outcomes are recorded. Until then the UI should call the number a ranking, not a probability. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, FINAL-PLAN.md — the reconciliation)_
- **[OPEN]** rubric_version / prompt_version on every score and draft — Makes re-derivation well-defined when the rubric changes. Cheap column; not yet added. _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[GAP]** ~20 hand-labelled postings re-scored on every prompt change — The test suite for the part that has no obvious test. Guards calibration drift. _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing)_

### 6 · Contacts — Per high-match company — GitHub commits → site scrape → learned pattern → Hunter (only if no pattern) → MX check.
- **[GAP]** Apify no-cookies LinkedIn employee harvest, budget-gated at $0.50/day, cached 24h — harvestapi~linkedin-company-employees ≈ $0.15 per 50 people; only for graded companies with no GitHub org. Pinned actor version in the cache key; rows enter as scraped until pattern inference resolves an email. Never touches our own LinkedIn session. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[GAP]** Clickable provenance — the HN comment, the board endpoint, the commit SHA behind every lead — A verification trail on the job detail page showing exactly why a company was scraped and how a contact's email was found. Contact.source and github are stored; the SHA and the UI are not. _(from: GEMINI.md — sequential agents, guardrails, provenance)_
- **[BUILT]** Company team/about/careers page scrape — Names, roles and recruiter aliases from the company's own pages. The fallback for companies with no public code. _(in jobhunter.contacts)_ _(from: PLAN.md — the original build plan, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[DROPPED]** Findymail / Anymailfinder + MillionVerifier credits — ₹3–5k/mo for the remainder after free sources. Not spent — free tiers are far above the 20% revisit threshold for companies worth contacting. _(from: JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal)_
- **[OPEN]** GitHub personal access token — 60/hr → 5,000/hr — Single highest-leverage config change available; contacts.github_token is unset. _(from: JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, FINAL-PLAN.md — the reconciliation)_
- **[BUILT]** GitHub repo-commits endpoint → commit.author.email — The repo commits endpoint still carries author emails; one call covers dozens of engineers, ~10–16 calls per company. Measured 100% for companies with public repos, 0% without — bimodal, not 20–40%. _(in jobhunter.contacts)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, GEMINI.md — sequential agents, guardrails, provenance)_
- **[BUILT]** Hunter.io free tier (25/mo) spent on patterns, not people — One domain-search call generalises to every name at that company. Only called when steps 1–3 left no pattern. _(in jobhunter.contacts)_ _(from: PLAN.md — the original build plan, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing)_
- **[BUILT]** Learn the company's email pattern, apply it to every other name, MX-check — first.last@ / flast@ / first@ learned from verified examples; results always labelled pattern-guessed. _(in jobhunter.contacts)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** MX lookup always; SMTP RCPT probe off — Residential ISPs block port 25, Google/Microsoft accept every recipient at the edge so a 250 proves nothing, and probing risks an IP listing. Built on by default — corrected in D7. _(in jobhunter.contacts)_ _(from: JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, FINAL-PLAN.md — the reconciliation)_
- **[GAP]** Manual LinkedIn alumni sourcing → CSV → pipeline resolves emails and drafts — The alumni filter is the tier-1 goldmine and the only safe way to use LinkedIn. Ten minutes a week, by hand, feeds the automated pipeline. _(from: JOBHUNTER-TOOLS.md — tools, services and relationship sourcing)_
- **[BUILT]** No public code and no team page → skip the company, don't guess — Better to skip a company than burn a send on a fabricated address. _(in jobhunter.contacts)_ _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, FINAL-PLAN.md — the reconciliation)_
- **[GAP]** Rank contacts by who can refer — engineers with verified email, then EMs, founders, recruiters last — Replaces plain confidence ordering in the drafter and the contacts view. Warmth tiers sit on top when built. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[GAP]** Role classifier — engineer | senior_engineer | manager | founder | recruiter | other, plus seniority — Deterministic keyword table on headline/bio first (SDE II, Staff, Founder, Talent, People), LLM only for the residue, unknown is kept as other and never dropped. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[DROPPED]** SMTP RCPT verification probe — Unreliable and can get the IP blocked. TOOLS.md said don't; the build had it on; now off. _(from: PLAN.md — the original build plan)_
- **[PARTIAL]** Verified-first ordering, then role relevance — Confidence tier first; engineers on the team you'd join beat recruiters. Seniority ordering (mid-level replies more than VPs) not built. _(in jobhunter.api, jobhunter.outreach)_ _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[GAP]** Warmth ladder — alumni > shared OSS > same city > same stack > content > cold > recruiter — Rank contacts by relationship strength and sort the review queue by warmth, not job score. Probably worth more than any other single feature; costs warmth_tier + warmth_evidence columns and a ranking function. _(from: JOBHUNTER-TOOLS.md — tools, services and relationship sourcing)_
- **[GAP]** apify.py — PINNED_ACTORS in ~ form with version, output_model and estimated cost; run-sync endpoint; 30-day cache keyed on (actor, version, input hash); budget pre-flight; ?maxCharge; shape mismatch non-retryable; Bearer only — gtm-arch §9 and zon-arch §13.1 merged. Called only for scored companies with no GitHub org. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, zon-arch.md — the MVRX portal)_
- **[BUILT]** verified | pattern-guessed | scraped — never defaulted, never collapsed — Named for what the user must distinguish when spending 25 sends. Badge everywhere a contact appears; sorts the queue. _(in jobhunter.db, jobhunter.contacts)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, IDEA.md — the problem, stated plainly, FINAL-PLAN.md — the reconciliation)_

### 7 · Research — Their repos + bio → one honest hook, null if the material is thin.
- **[GAP]** Brave Search + politeness-core fetch + cheap model → observations and facts that each carry an artifact span; uncited items dropped; source quality classified separately — CHOKEPOINTS research_brave.py and gtm-arch web_search/web_fetch (SSRF-guarded, no redirects). The drafter never receives an uncited observation. _(from: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** Researcher reads their repos + bio → one honest hook, or null — The hook is null if the material is too thin. Never invented. GitLab contact came back with hook=null and a company-level note — that's the design working. _(in jobhunter.outreach)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[GAP]** Scraped bios and pages enter prompts wrapped as untrusted data, never instructions — A bio that says "ignore previous instructions" must not work on the researcher or the drafter. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet)_

### 8 · Draft — 90–130 words, plain text, local model.
- **[GAP]** A contact is written to by at most one of us per 30 days; one of us per company per role — Enforced in drafter.draft_for, not in a prompt. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[DROPPED]** Claude Sonnet for drafts (local for everything else) — "Local 8B prose is noticeably stiffer." Dropped in D3; the seam in llm.py is kept. Pull it first if reply rates are bad — not before. _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing)_
- **[DROPPED]** Drafts written by the local model — 90–130 words, plain text — The review queue catches stiff prose; a stiff draft you edit is not a failure mode, an unreviewed one would be. _(in jobhunter.outreach)_ _(from: PLAN.md — the original build plan, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[OPEN]** Evidence spans on every claim + a deterministic auditor that strips, never rewrites — Persona Reader emits only cited observations; Drafter maps sentences to spans; auditor checks existence then support. Heavy — the cheap validator is preferred first. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, GEMINI.md — sequential agents, guardrails, provenance)_
- **[GAP]** Faithfulness gate (span exists · span supports) + publish gate (every proper noun and number matches a span or profile field) — strip, never rewrite — gtm-arch faithfulness.py plus CHOKEPOINTS publish_gate.py applied to outbound text. A draft left generic is labelled generic and shown as such. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, GEMINI.md — sequential agents, guardrails, provenance)_
- **[DROPPED]** OpenAI-compatible cheap hosted model (Kimi / DeepSeek via OpenRouter) behind llm.py, drafts only — ₹100–400/month at our volume. Pull only if local drafts are the bottleneck (D3). CHOKEPOINTS' ask_llm pattern — one client fronting many cheap providers, think-stripping, max_tokens budgeted for reasoning. _(from: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[GAP]** Per-task routing in llm.py — draft / followup / review to OpenRouter, score / reply / research local, local fallback always — One OpenAI-compatible key; daily call cap and token ledger in Setting; no key or any error falls back to qwen3:4b. A prototype was wired and reverted on 2026-08-30 pending approval (build step 8). _(from: FINAL-PLAN-V2.md — the plan against the restated problem, CHOKEPOINTS.md — a keyless scraper run at the source's rate limit)_
- **[GAP]** Proper nouns + numbers in the draft must appear in the researcher's input; mismatches flagged in the review UI — The cheap 80% of evidence spans. A 4B model will eventually invent a repo name; prompts drift, validators don't. _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, FINAL-PLAN.md — the reconciliation)_
- **[OPEN]** Voice check against the corpus of previously approved drafts — If you find yourself approving without editing, drafts have become templates. No corpus yet. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_

### 9 · Review queue — the human gate — Edit · approve · reject. The only path to send; budget visible and depleting.
- **[GAP]** AI-tell vocabulary and phrase lint on drafts — no em dashes, no "delve / leverage / robust", contractions, grade 6–8 — zon-arch's ~70-word list plus banned corporate phrases, applied by the approval assistant and _clean_body. The drafter already strips em dashes. _(from: zon-arch.md — the MVRX portal)_
- **[GAP]** Approval assistant — a review worker that flags each draft for fabrication, tone, length and the one-ask rule; never approves — Writes email.review_flags; the queue shows them beside the draft. The human still presses approve. Runs on the hosted tier when enabled, locally otherwise. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[GAP]** Drafts expire after 7 days — A stale draft about a filled role makes the sender look inattentive — worse than not sending. One column, one check. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[OPEN]** Keyboard-driven review (j k a r), inline edit — Most drafts need one line changed, not a rewrite. Ten minutes a day or the tool has failed. _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal)_
- **[BUILT]** Next.js dashboard — Overview · Leads · Contacts · Queue · Tracker · Replies · Settings — Reads and writes only through the REST API. _(in dashboard)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, FINAL-PLAN.md — the reconciliation)_
- **[BUILT]** Remaining daily budget visible and depleting on every screen — Scarcity has to be felt at review time, not discovered at send time. _(in dashboard)_ _(from: IDEA.md — the problem, stated plainly, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** Review queue — edit · approve · reject; the only path to send — The one place a human is required and the only place a human adds anything. send_email refuses anything not approved. _(in jobhunter.outreach, jobhunter.api, dashboard)_ _(from: IDEA.md — the problem, stated plainly, PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, GEMINI.md — sequential agents, guardrails, provenance, FINAL-PLAN.md — the reconciliation)_
- **[GAP]** Telegram bot — proposal cards with Approve / Edit / Reject / Skip, atomic pending→processing claim, Edit becomes edited_body, budget alerts at 80%, morning report — zon-arch's Slack-as-UI translated to a free bot for two people. Telegram records the decision; only the API's approval handler acts on it. _(from: zon-arch.md — the MVRX portal, FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model)_
- **[GAP]** fact_proposal + draft_proposal — every agent write is a proposal; facts promote at ≥ 0.85, review 0.70–0.85, review-with-reason below; drafts always HITL; never auto-reject — gtm-arch's promotion path. Contact role, seniority, email and employer are facts with evidence; drafts carry claims with spans. _(from: gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model)_
- **[BUILT]** macOS notifications for new high matches and replies — osascript; plus a "New" section in the dashboard. _(in jobhunter.notify)_ _(from: PLAN.md — the original build plan)_

### 10 · Send — Gmail API, ≤25/day, 10:00–19:00, 45–210 s stagger, plain text.
- **[GAP]** At most 5 of the 25 daily sends to pattern-guessed addresses — Bounces are the main way this system damages the account it depends on. _(from: MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** Daily cap as a count — sent rows since local midnight — Nearly right. The decrement isn't in the same transaction as the Gmail call, so a crash between send and DB write loses a send from the count. _(in jobhunter.outreach)_ _(from: PLAN.md — the original build plan)_
- **[PARTIAL]** Gmail API (OAuth) from the user's own account — needed for threading and reply reads — Not SMTP, not Resend/SendGrid. Built; untested end to end because OAuth credentials don't exist yet. _(in jobhunter.outreach)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[BUILT]** Send 10:00–19:00 local, 45–210 s stagger, plain text, no pixels — A personal Gmail must look like a person. No HTML, no tracking, no link shortener. _(in jobhunter.outreach)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[GAP]** send_budget(date, cap, used) row, locked and decremented inside the send transaction — Both ARCHITECTURE (unique (date, slot)) and ORCHESTRATION specify it. Do this before the first real send — the failure it prevents damages the Gmail account. _(from: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_

### 11 · Track replies — Poll threads → classify positive | negative | closed | neutral; ambiguous → neutral → human reads it.
- **[PARTIAL]** Funnel with numbers — reply rate by role type, framing, confidence tier — What turns job hunting from hopeful gestures into something adjustable. tracker.funnel() gives the counts; the breakdowns aren't built. _(in jobhunter.outreach, dashboard)_ _(from: IDEA.md — the problem, stated plainly, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers)_
- **[PARTIAL]** Replies classified positive | negative | closed | neutral; unclear goes to the human — A forced guess on a hedged reply is worse than an admission. Built, untested against real mail. _(in jobhunter.outreach)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, IDEA.md — the problem, stated plainly)_

### 12 · Follow up — Five days silent → exactly one follow-up → back to the review queue.
- **[PARTIAL]** Exactly one follow-up after 5 silent days, back through the review queue — A second follow-up converts almost nothing and costs the relationship. followup_sent is set at queue time. _(in jobhunter.outreach)_ _(from: PLAN.md — the original build plan, JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, IDEA.md — the problem, stated plainly)_

### 13 · Schedule — A positive reply with a link, a slot or a deadline becomes an event on our calendar with a notification; conflicts become a reschedule draft in the review queue.
- **[GAP]** Calendar sync with syncToken; 410 → full resync ±7 days; notify at ≤30 min once via notified_at — The two failure modes — expired token and double notification — solved verbatim from zon-arch §16.8. _(from: zon-arch.md — the MVRX portal)_
- **[OPEN]** One-page prep checklist per confirmed interview from the job's gaps and the company's product — Small on purpose. The pipeline gets the screen; it does not pass it. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[GAP]** Schedule extractor — a positive reply becomes {intent, proposed_times, link, deadline, needs_action} — Times parsed with both timezones (the classic failure is a "4pm" that was theirs). Nothing is confirmed to the other party by the machine. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[GAP]** Two companies on one slot → a reschedule reply drafted into the review queue — Conflict detection against the live calendar; the draft is approved by us like any other email. _(from: FINAL-PLAN-V2.md — the plan against the restated problem)_
- **[GAP]** event table + Google Calendar (same OAuth, calendar scope) + macOS/Telegram notification — Confirmed slots become calendar events; assessments get a block two days before the deadline; "send your resume" becomes needs_action. Design lifted from zon-arch's calendar sync and meeting notifier. _(from: zon-arch.md — the MVRX portal, FINAL-PLAN-V2.md — the plan against the restated problem)_

## Gaps, in build order

0. **V3** (`gap:16-permission-gate`) — Everything later is gated by it. Wrap Hunter and send_email first.
   - builds: Permission gate — tiers 0–4, permissions.yaml grants, approval_request + action_log, pending/grant/deny CLI and dashboard tab; action_log — every tier 1–3 call with cost, candidate and outcome; agent_run + agent_run_cost + apify_run_log — every invocation a row with phases, status, tokens, INR, error, parent_run_id; budgets.py — every timeout, threshold and cap in one file, pinned by a test; no AUTO_REJECT symbol
   - protects: Nothing sends without a human; 25/day, irreversibly spent, visible everywhere
0. **V3** (`gap:18-model-layer`) — Makes the machine safe to run on. openrouter.py with aliases and a cost row per call; llm.py deleted; grep -ri ollama returns 0.
   - builds: OpenRouter-only model layer — aliases cheap / judge / writer, num_retries 0, think-stripping, a cost row per call, budget gate, no key → stage does not run; pydantic-ai agent runtime — typed I/O, narrow tool surface, enforce_capability first line of every tool, budget triple, EXECUTE_OUTREACH held by nobody; CI greps and tests — no ollama import, no network outside the four adapters, no untimed wait, Apify Bearer only, no AUTO_REJECT, EXECUTE_OUTREACH unheld, every tool has a capability, every constant pinned
   - protects: 25/day, irreversibly spent, visible everywhere
0. **V3** (`gap:19-scoring-v3`) — The cost gradient with no model at the wide end.
   - builds: Model-free lexical fit — rarity-weighted vocabulary overlap between resume and JD, stemmed, stop-listed; Triage agent on the cheap alias — worth scoring? region? remote? sponsorship stated?; Fifth rubric dimension — does this posting allow remote-from-India, sponsor, or sit where a new-grad visa is realistic; Target regions with per-region pay floors — US/UK/DE any well-paid, India ≥ ₹30 LPA on-site
1. **Gmail OAuth credentials + end-to-end dry run to a second inbox** (`gap:0-gmail-e2e`) — The only untested path. Everything downstream is theory until one email sends, gets replied to, and classifies correctly.
   - builds: Gmail API (OAuth) from the user's own account — needed for threading and reply reads; Replies classified positive | negative | closed | neutral; unclear goes to the human; Exactly one follow-up after 5 silent days, back through the review queue
2. **GAP-1 — Send budget should be a ledger, not a count** (`gap:1-send-ledger`) — A crash between the Gmail call and the DB write loses a send from the count. Small change; the failure it prevents damages the Gmail account. Do it before the first real send.
   - builds: send_budget(date, cap, used) row, locked and decremented inside the send transaction
   - protects: 25/day, irreversibly spent, visible everywhere
3. **GAP-4 — guessed-send cap and draft expiry** (`gap:4-guessed-cap-expiry`) — At most 5 of 25 to unverified addresses; drafts expire at 7 days. Two small checks, both protect the account.
   - builds: At most 5 of the 25 daily sends to pattern-guessed addresses; Drafts expire after 7 days
   - protects: Every contact carries a confidence tier, never collapsed; 25/day, irreversibly spent, visible everywhere
4. **GAP-2 — Anti-fabrication is a prompt, not a check** (`gap:2-fabrication-check`) — Highest-value correctness gap. Extract proper nouns and numbers from the draft, check each against the researcher's input, flag mismatches in the review UI. Before volume — one invented claim is worse than fifty generic emails.
   - builds: Proper nouns + numbers in the draft must appear in the researcher's input; mismatches flagged in the review UI
   - protects: Nothing is invented
4. **V3** (`gap:20-proposals-gates`) — Every agent write becomes a proposal; drafts carry claims with spans; unsupported specifics are stripped.
   - builds: fact_proposal + draft_proposal — every agent write is a proposal; facts promote at ≥ 0.85, review 0.70–0.85, review-with-reason below; drafts always HITL; never auto-reject; artifact table — every fetched document append-only with content_hash; evidence spans point here; Faithfulness gate (span exists · span supports) + publish gate (every proper noun and number matches a span or profile field) — strip, never rewrite; Approval assistant — a review worker that flags each draft for fabrication, tone, length and the one-ask rule; never approves; AI-tell vocabulary and phrase lint on drafts — no em dashes, no "delve / leverage / robust", contractions, grade 6–8
   - protects: Nothing is invented; Nothing sends without a human
4. **V3** (`gap:21-telegram`) — Approve from the phone; a double tap yields one send.
   - builds: Telegram bot — proposal cards with Approve / Edit / Reject / Skip, atomic pending→processing claim, Edit becomes edited_body, budget alerts at 80%, morning report; Daily report — sources at 0, scores, people by channel, drafts owed, sends left, replies by class, events today, money spent by vendor and alias against caps
5. **GAP-6 — No tests, no layering lint** (`gap:6-tests-lint`) — Salary parsing against a fixture table, scraper parsing against recorded JSON, ~20 hand-labelled scoring fixtures. Cheapest insurance on the two subtlest pieces of logic.
   - builds: Fixture-driven tests — salary table, recorded scraper JSON, hand-labelled scores; ~20 hand-labelled postings re-scored on every prompt change; Layering rule enforced by a lint script
5. **V3** (`gap:22-scraping-engine`) — The CHOKEPOINTS engine shape.
   - builds: fetch.py — the only place the network is touched for free sources; robots when real, per-host rate, disk cache by URL hash, real UA, 20 s / 2 MB bounds; CI grep for any other httpx call; tick.py under launchd — hourly, fcntl-locked, spends this hour's rate budget and today's money budget, most-stale work first, source_health with backoff, exits in minutes; Every channel writes staging; promote previews by default, --apply refuses on a failing audit and restores the backup; Funding-round RSS → company names → ATS probe → staged registry entries; Every multi-item stage appends and fsyncs per item and skips ids already present on restart — a relaunch never re-spends a paid call
6. **GAP-3 — Warmth tiers + alumni CSV** (`gap:3-warmth-tiers`) — The biggest conversion lever, deliberately after correctness. A message to a fellow Newton School alum converts differently to a cold one.
   - builds: Warmth ladder — alumni > shared OSS > same city > same stack > content > cold > recruiter; Manual LinkedIn alumni sourcing → CSV → pipeline resolves emails and drafts
   - protects: 25/day, irreversibly spent, visible everywhere
6. **V3** (`gap:23-people-v3`) — Three channels for people; only cited observations reach the drafter.
   - builds: apify.py — PINNED_ACTORS in ~ form with version, output_model and estimated cost; run-sync endpoint; 30-day cache keyed on (actor, version, input hash); budget pre-flight; ?maxCharge; shape mismatch non-retryable; Bearer only; Apify no-cookies LinkedIn employee harvest, budget-gated at $0.50/day, cached 24h; Role classifier — engineer | senior_engineer | manager | founder | recruiter | other, plus seniority; Rank contacts by who can refer — engineers with verified email, then EMs, founders, recruiters last; Brave Search + politeness-core fetch + cheap model → observations and facts that each carry an artifact span; uncited items dropped; source quality classified separately; Scraped bios and pages enter prompts wrapped as untrusted data, never instructions
7. **GAP-7 — Calibration is unmeasured** (`gap:7-calibration`) — Needs an outcomes table and the discipline to record what happened. Only meaningful after a month of real replies.
   - builds: outcomes table + calibration against real screens; rubric_version / prompt_version on every score and draft
8. **GAP-5 — Playwright scrapers, or the manual registry route** (`gap:5-playwright`) — Last, or never. Browsing the Indian boards by hand and pasting company names is cheaper and safer.
   - builds: Browse India boards by hand, paste company names into the registry; Playwright scrapers for Wellfound, Cutshort, Instahyre
9. **Source freshness alarm** (`gap:8-freshness-alarm`) — A source returning 0 twice running should be reported as a failure, not a quiet day. per_source is recorded; nothing reads it.
   - builds: A source returning 0 twice running is reported as a failure
12. **V2** (`gap:11-two-candidates`) — Three evenings. Doubles the ceiling without doubling the spam.
   - builds: Two candidates, one pipeline — candidate table, per-candidate scores, drafts, Gmail and budgets; A contact is written to by at most one of us per 30 days; one of us per company per role
14. **V2** (`gap:13-calendar`) — Four evenings. The genuinely new subsystem; the thing that makes a "yes" not get lost.
   - builds: Schedule extractor — a positive reply becomes {intent, proposed_times, link, deadline, needs_action}; event table + Google Calendar (same OAuth, calendar scope) + macOS/Telegram notification; Two companies on one slot → a reschedule reply drafted into the review queue
   - protects: Nothing sends without a human
16. **V2** (`gap:15-apify-harvest`) — Two evenings, ≤ ₹1,000/month. People at companies with no public code. Measure verified-email yield after pattern inference before keeping it.
   - builds: Apify no-cookies LinkedIn employee harvest, budget-gated at $0.50/day, cached 24h; Apify response cache keyed on (actor, version, input hash) with TTL, plus a daily cost ledger; Scraped bios and pages enter prompts wrapped as untrusted data, never instructions

## Failure modes, most likely first

- **A source breaks silently** — Board changes shape, scraper returns 0, nothing errors. Signal — one source's count drops to 0 while others are normal. _(mitigated by: One dead source never kills a run, A source returning 0 twice running is reported as a failure)_
- **The human stops reviewing** — Queue depth grows, approvals go to zero. A backlog of unread drafts is pure waste. _(mitigated by: Drafts expire after 7 days, Keyboard-driven review (j k a r), inline edit)_
- **The local model degrades on long runs** — Ollama evicting/reloading under 8 GB pressure — a scoring pass suddenly takes minutes per job. Fixed by fixed num_ctx + keep_alive; re-check if a third model is ever added. _(mitigated by: qwen3:4b + nomic-embed-text, resident together, One thin llm.py — the only place a model is called)_
- **Bounces damage the sending account** — Guessed addresses fail; Gmail notices the pattern. _(mitigated by: At most 5 of the 25 daily sends to pattern-guessed addresses, verified | pattern-guessed | scraped — never defaulted, never collapsed, Verified-first ordering, then role relevance)_
- **Drafts read as templates** — Reply rate flat and low. If you find yourself approving without editing, that's the signal. _(mitigated by: Review queue — edit · approve · reject; the only path to send, Voice check against the corpus of previously approved drafts, Warmth ladder — alumni > shared OSS > same city > same stack > content > cold > recruiter)_
- **GitHub rate limit stalls contact discovery** — 60/hr unauthenticated stalls after ~3 companies. _(mitigated by: GitHub personal access token — 60/hr → 5,000/hr)_
- **Commit mining yields nothing for companies without public code** — Expected, not a bug. Site scrape is the fallback; if both fail, skip rather than guess. _(mitigated by: Company team/about/careers page scrape, No public code and no team page → skip the company, don't guess)_

## Still open

- **Does 4B stay good enough as volume grows?** — Ranking is right today. If reply rates are poor and drafts read stiff — better local model on a bigger machine first, then hosted model for drafting only.
- **Does review actually take ten minutes?** — Unmeasured — there has been one draft. Measure before scaling to 25/day.
- **Warmth or score as the primary sort?** — TOOLS argues warmth. Untested because warmth isn't built.
- **Is the 70th-percentile prefilter the right number?** — Keeps the top 30% (~330 of 1,100). Chosen from the similarity distribution, not from outcomes.
- **What happens when a company replies from a different address?** — Thread matching by gmail_thread_id handles most of it; person-level identity across addresses is unsolved.
- **Is $0.15/company of Apify data worth more than the same evenings on warmth tiers?** — Measure the verified-email rate of Apify-sourced contacts after pattern inference before deciding.
- **Should low-grade (< 40) roles ever be auto-drafted?** — The plan says no, MOTIV says "still send". Resolve after a month of outcomes.

## The architecture drafts and what each contributed

- **PLAN.md — the original build plan** (`system docs/PLAN.md`) — Python 3.12 + SQLModel + FastAPI pipeline, Next.js dashboard, Qwen3-4B via Ollama on 8 GB, seven free sources, five build phases. This is the draft the code was actually built from.
  - proposed 30 features: 20 built, 4 dropped, 1 open, 5 partial
- **JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal** (`system docs/JOBHUNTER-ARCHITECTURE.md`) — Next.js + Drizzle + a Node worker; copies the portal's layering lint, HITL state machine and background-only long work; fixes its silent failures, missing tests and missing cost column. Hosted Claude for drafts, a (date, slot) send ledger, a proper-noun fabrication validator.
  - proposed 34 features: 17 built, 5 dropped, 5 gap, 3 open, 4 partial
- **GEMINI.md — sequential agents, guardrails, provenance** (`system docs/GEMINI.md`) — Four single-purpose agents passing typed handoffs, two runtime guardrails (tool-loop limit 5, Pydantic validation with 2 retries), and a clickable Provenance Panel showing exactly which HN comment or git commit produced each lead.
  - proposed 9 features: 6 built, 2 gap, 1 open
- **IDEA.md — the problem, stated plainly** (`system docs/IDEA.md`) — The narrative brief. Defines the funnel shape, the honest-score rule, the confidence labels, the 25/day ceiling and the hard non-goals. Every later draft agrees with it.
  - proposed 7 features: 4 built, 3 partial
- **MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers** (`system docs/MULTI-AGENT-ORCHESTRATION.md`) — A deterministic conductor over 5 LLM supervisors over 18 narrow workers with typed envelopes, borrowed from gtm-os. Contributes the ideas that survived — send budget as a ledger, evidence-bound drafting, immutable artifacts, no auto-reject, guessed-send cap, draft expiry, outcomes for calibration — and the hierarchy that did not.
  - proposed 53 features: 20 built, 7 dropped, 14 gap, 6 open, 6 partial
- **JOBHUNTER-TOOLS.md — tools, services and relationship sourcing** (`system docs/JOBHUNTER-TOOLS.md`) — The what-do-I-sign-up-for doc. Free ATS APIs, GitHub as the relationship graph, an email-finding waterfall with paid tiers, and the warmth ladder (alumni > shared OSS > same city > stack > content > cold > recruiter) — its single best idea.
  - proposed 24 features: 10 built, 6 dropped, 5 gap, 2 open, 1 partial
- **FINAL-PLAN.md — the reconciliation** (`system docs/FINAL-PLAN.md`) — Supersedes the three competing drafts. States nine decisions against code that is actually running, measured on the live database, and lists seven gaps in build order. When in doubt, this document wins.
  - proposed 16 features: 8 built, 2 dropped, 3 gap, 2 open, 1 partial · supersedes JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers
- **CHOKEPOINTS.md — a keyless scraper run at the source's rate limit** (`system docs/CHOKEPOINTS.md`) — Rebuild manual for a PE deal-sourcing atlas. Transfers the scraping engine ideas — the rate budget as the governing constraint, hourly bounded ticks under launchd, staging + audit-gated promotion, freshness alarms, source-quality classification, cheap hosted models behind one CLI, and a table of bugs already paid for.
  - proposed 16 features: 1 dropped, 15 gap
- **gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet** (`system docs/gtm-arch.md`) — Multi-tenant SaaS spec. Transfers the Apify no-cookies LinkedIn actors with pinned versions, per-call costs, a daily budget gate and a 24h cache; the untrusted-content wrapper; no-auto-reject; and the named-but-unheld send capability. Everything multi-tenant is not taken.
  - proposed 16 features: 16 gap
- **FINAL-PLAN-V2.md — the plan against the restated problem** (`system docs/FINAL-PLAN-V2.md`) — Keeps D1–D10, adds D11–D15 — SQLite not CSV, LinkedIn only via no-cookies public channels, agents are workers not supervisors, regions/pay/visa feasibility in config and the rubric, two candidates with a cross-candidate cooldown. Five changes — targeting, people and roles, two candidates, replies → calendar, operations — with a twelve-step build order and a ≤ ₹1,400/month cost sheet.
  - proposed 20 features: 1 built, 2 dropped, 16 gap, 1 open · supersedes FINAL-PLAN.md — the reconciliation
- **zon-arch.md — the MVRX portal** (`system docs/zon-arch.md`) — Agency portal rebuild spec. Transfers the calendar sync (syncToken, events table with attendees, meeting notifier), the Apify response cache keyed on actor + input hash with TTL, and the long-jobs-never-in-handlers rule. Slack-as-UI and the content tooling are not taken.
  - proposed 7 features: 7 gap
- **FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model** (`system docs/FINAL-PLAN-V3.md`) — Ollama removed entirely. OpenRouter-only model layer with aliases, num_retries 0, cost rows and budget gates; pydantic-ai agents with capabilities and budget triples that can only propose; fact and draft proposals with thresholds and no auto-reject; faithfulness and publish gates; three scraping channels (keyless politeness core, Apify no-cookies, Brave research) behind staging → audit → promote; launchd ticks; Telegram review cards with atomic claims; calendar syncToken flow; permission tiers; 13 numbered build steps.
  - proposed 8 features: 8 gap · supersedes FINAL-PLAN-V2.md — the plan against the restated problem, FINAL-PLAN.md — the reconciliation

## Module map (nothing imports upward)

- `jobhunter/db.py` — SQLModel models Company/Job/Contact/Email/Reply/Setting, engine, kv helpers.
- `jobhunter/llm.py` — The only place a model is called — Ollama chat, JSON mode with retry, embeddings, think-stripping.
- `jobhunter/normalize.py` — Relevance rules, dedup fingerprint, salary → INR LPA, mojibake repair. → jobhunter.llm
- `jobhunter/notify.py` — macOS notifications for high matches and replies. → jobhunter.db
- `jobhunter/resume.py` — PDF/MD → profile.json via LLM, pulling hyperlinks from PDF annotations. → jobhunter.llm
- `jobhunter/contacts/` — github_miner · site_scraper · patterns · hunter · verify; __init__ runs the waterfall. → jobhunter.db, jobhunter.scrapers
- `jobhunter/outreach/` — researcher · drafter · gmail · sender · tracker. → jobhunter.db, jobhunter.llm
- `jobhunter/scrapers/` — base.py (RawJob + async HTTP), discover.py (ATS probe), greenhouse · lever · ashby · hn_hiring · remoteok · wwr · yc. → jobhunter.normalize
- `jobhunter/kg/` — Knowledge graph — store (SQLite nodes/edges/FTS), context loader, data sync, compose, brief, export. → jobhunter.db
- `jobhunter/matcher.py` — Resume vector cache, percentile prefilter, rubric scoring with hard-rule caps, counts. → jobhunter.llm, jobhunter.db
- `jobhunter/pipeline.py` — Scrape orchestration, company sync from companies.yaml, persist with dedup, LLM salary backfill. → jobhunter.scrapers, jobhunter.normalize, jobhunter.db
- `jobhunter/scheduler.py` — APScheduler — daily scrape+score cycle, hourly reply poll; sequential on purpose (8 GB). → jobhunter.pipeline, jobhunter.matcher, jobhunter.outreach, jobhunter.kg
- `jobhunter/api.py` — FastAPI — reads, actions dispatched to background threads with task ids, config, kg routes. → jobhunter.pipeline, jobhunter.matcher, jobhunter.contacts, jobhunter.outreach, jobhunter.kg
- `scripts/run.py` — Typer CLI — doctor profile scrape score find-contacts draft gmail-auth send poll daily discover serve, plus the kg sub-commands. → jobhunter.api
- `dashboard/` — Next.js 16 — Overview · Leads · Contacts · Queue · Tracker · Replies · Settings. Talks only to the REST API. Its own git repo. → jobhunter.api

## Session log (newest first)

- **2026-08-31** FINAL-PLAN-V3 written from gtm-arch, zon-arch and CHOKEPOINTS; Ollama removed from the plan entirely — Tejmul rejected v2 as "the old architecture with deltas" and ruled out Ollama (it wrecks the machine). v3 rebuilds on the three documents — OpenRouter-only aliases with ledgers and caps, pydantic-ai agents that can only propose, fact/draft proposals with thresholds, faithfulness and publish gates, politeness core + tick + staging/promote, Apify pin registry, Brave research rig, Telegram review cards, calendar syncToken flow. D18–D21 recorded; v2 gaps replaced by 13 numbered v3 steps awaiting approval. No app code changed. _(about: FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model, No Ollama, no local model, ever, D18 — No Ollama, no local model; OpenRouter is the only model provider, D19 — Agents are pydantic-ai workers that can only propose; capabilities and budgets are enforced by the runtime, not the prompt, D20 — Three scraping channels behind one politeness core, one staging area, one audit gate, driven by a launchd tick, D21 — Telegram is the second review surface, with the atomic-claim pattern)_
- **2026-08-30** zon-arch.md, gtm-arch.md and MULTI-AGENT-ORCHESTRATION.md read in full; CHOKEPOINTS.md too — The first pass had skipped zon-arch §10–24 and gtm-arch §15–18. Read now. Added to the plan — Apify client mechanics (sync endpoint, ~ ids, 30-day cache, non-retryable shape errors), the calendar syncToken/410/notified_at flow, the humanisation vocabulary, date injection into prompts, aliases not model ids, and seven more bugs-already-paid-for. _(about: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, FINAL-PLAN-V2.md — the plan against the restated problem, zon-arch.md — the MVRX portal)_
- **2026-08-30** Problem restated in knowledge/MOTIV.md; FINAL-PLAN-V2 written — Tejmul restated the problem — the US/UK/DE pay arbitrage, funded startups not hyped names, India only at ₹30 LPA+, 10–30 people per company with roles, replies → calendar, two candidates. CHOKEPOINTS.md, gtm-arch.md and zon-arch.md added to system docs and decomposed into the graph. V2 keeps D1–D10, adds D11–D15 and gaps 9–15. _(about: CHOKEPOINTS.md — a keyless scraper run at the source's rate limit, gtm-arch.md — gtm-os, budget-gated paid scraping and a HITL agent fleet, FINAL-PLAN-V2.md — the plan against the restated problem, zon-arch.md — the MVRX portal, Get referred into ₹30 LPA+ roles at funded US/UK/DE (and Indian) startups, 25 honest emails a day each)_
- **2026-08-30** Plan v3 complete and awaiting approval. Free-API bench added (Wikimedia/DBpedia, Companies House, OpenCorporates, Product Hunt). D2/D3 marked reversed in the graph. No app code written; still planning. _(about: FINAL-PLAN-V3.md — built from gtm-arch, zon-arch and CHOKEPOINTS; no local model)_
- **2026-08-30** Plan v2 finished for approval: MOTIV.md written, FINAL-PLAN-V2.md with D11-D17, tool inventory (25 tools), permission/guardrail system and a 14-step build protocol. OpenRouter prototype reverted; no app code changed this turn. Waiting for step approval. _(about: FINAL-PLAN-V2.md — the plan against the restated problem, D17 — Nothing runs or gets built without permission, Permission gate — tiers 0–4, permissions.yaml grants, approval_request + action_log, pending/grant/deny CLI and dashboard tab)_
- **2026-08-30** OpenRouter routing prototype reverted; permission system and tool inventory added to the plan — Tejmul asked for OpenRouter for the writing tasks, then asked for a plan-first, approval-gated process with a full tool list. The per-task routing that had been wired into llm.py/config was reverted (verified identical to HEAD). FINAL-PLAN-V2 gained D16 (OpenRouter writing tier), §12 tool inventory, §13 permission and guardrail system, §14 build protocol with 14 numbered steps awaiting approval. _(about: FINAL-PLAN-V2.md — the plan against the restated problem, D16 — OpenRouter for the writing tasks, local for everything else, both behind the permission gate, D17 — Nothing runs or gets built without permission, Permission gate — tiers 0–4, permissions.yaml grants, approval_request + action_log, pending/grant/deny CLI and dashboard tab)_
- **2026-08-30** Knowledge graph built and verified: 1,600+ nodes, search/show/path/compose/hubs/API all exercised. Docs moved to 'system docs/'. _(about: D10 — The knowledge graph lives in SQLite with NetworkX on top, not in a graph database, Knowledge graph — data + context in one queryable store)_
- **2026-08-30** Knowledge graph added — data + context layers in jobhunter.db — jobhunter/kg (store, context, sync, compose, brief, export), knowledge/context.yaml seed, `kg` CLI and /api/kg routes, scheduler syncs after each cycle, CLAUDE.md points new sessions at knowledge/BRIEF.md. _(about: A new session must not start from scratch, D10 — The knowledge graph lives in SQLite with NetworkX on top, not in a graph database, Knowledge graph — data + context in one queryable store)_
- **2026-08-29** FINAL-PLAN.md reconciles the drafts against running code — Measured on the live DB — 1,100 jobs stored of 11,282 scraped, 94 ATS boards verified, 102 scored, 26 high matches, 263 companies, 25/25 contacts verified, 1 draft. Working end to end through draft; approve → send → reply → follow-up untested. _(about: FINAL-PLAN.md — the reconciliation)_
- **2026-08-02** Config tuned for the real machine — qwen3:4b instead of 14B, percentile prefilter instead of absolute threshold, SMTP probe turned off, keep_alive 30m, fixed num_ctx. _(about: D2 — (reversed by D18) qwen3:4b, the size the machine can actually hold, D7 — SMTP RCPT probing is off; TOOLS.md was right, Embedding prefilter by percentile, not threshold)_
- **2026-08-01** Three competing architecture drafts written after reading the MVRX portal — JOBHUNTER-ARCHITECTURE (TS, portal lessons), JOBHUNTER-TOOLS (services + warmth ladder), MULTI-AGENT-ORCHESTRATION (gtm-os patterns), GEMINI (guardrails + provenance), plus IDEA.md. They disagree on language, model strategy and orchestration. _(about: JOBHUNTER-ARCHITECTURE.md — lessons from the MVRX portal, GEMINI.md — sequential agents, guardrails, provenance, IDEA.md — the problem, stated plainly, MULTI-AGENT-ORCHESTRATION.md — conductor, supervisors, workers, JOBHUNTER-TOOLS.md — tools, services and relationship sourcing)_
- **2026-07-31** Project scaffolded — first commit — Python 3.12 + uv, SQLModel schema, Ollama client, seven Tier-1 scrapers, companies.yaml seed, FastAPI + Next.js dashboard skeleton. PLAN.md written the same day. _(about: PLAN.md — the original build plan)_
